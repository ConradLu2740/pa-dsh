{
  "name": "pa-dsh",
  "version": "0.1.0",
  "private": true,
  "description": "ProactiveAgent → DeepSeek Harness 插件组（monorepo）",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit || true"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0"
  }
}
